name: SocialPerks Outreach — Casablanca

on:
  schedule:
    - cron: '30 7 * * *'   # ogni giorno alle 09:30 ora di Parigi (UTC+2) — sfalsato di 30 min
  workflow_dispatch:         # permette avvio manuale dal sito GitHub

jobs:
  outreach:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Installa dipendenze
        run: pip install -r requirements_outreach.txt

      - name: Esegui outreach Casablanca
        env:
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python outreach_casablanca.py run

      - name: Salva stato tracking
        run: |
          git config user.name "SocialPerks Bot"
          git config user.email "socialperks-bot@github.com"
          git add -f outreach_tracking_casablanca.db
          git diff --cached --quiet || git commit -m "Update outreach tracking Casablanca [skip ci]"
          git push
